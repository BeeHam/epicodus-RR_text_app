<h1>Contacts</h1>

<ul>
  <% @contacts.each do |contact| %>
  <li><%= link_to contact.name, contact_path(contact) %>, <%= contact.phone_number %></li>
  <% end %>
</ul>

<%= link_to "Add a contact", new_contact_path %>
